package server

// Auth credential management is now handled by the aiauth package.
// The aiauth.Store resolves keys with automatic OAuth token refresh,
// so periodic syncing is no longer needed here.
